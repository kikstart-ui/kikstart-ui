import { UiBrand } from '@kikstart-ui/ui-brand'

export const config: { layout: { brand: UiBrand } } = {
  layout: {
    brand: {
      logo: 'assets/logo.svg',
      name: 'kikstart',
      product: 'ui',
      separator: '|',
      description: 'Set of ready to use declarative Angular UI components.',
    },
  },
}
